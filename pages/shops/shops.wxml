<!--logs.wxml-->
<view>
  <block wx:for="{{shops}}" wx:key="shopId" wx:for-item="shop">
    <view class="item-container">
      <view style='line-height:82rpx;'>
        {{shop.shopId}} : {{shop.shopName}} : {{shop.shopAddr}}:{{shop.shopDesc}}
      </view>
    </view>
  </block>
</view>